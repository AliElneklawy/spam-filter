services:
  web_app:
    build: .
    command: uv run uvicorn src.app:app --host 0.0.0.0 --port 8000
    ports: 
      - "8000:8000"
    environment:
      - PYTHONPATH=/app/src
    restart: unless-stopped
  tg_bot:
    build: .
    command: uv run src/tg_bot.py
    restart: unless-stopped